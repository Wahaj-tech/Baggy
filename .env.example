# .env.example — copy to .env and fill with real values
PORT=3000
MONGO_URI=mongodb://localhost:27017/baggy
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=7d
SESSION_SECRET=another_long_secret_for_sessions_and_flash
# Optional: cloud storage keys
# AWS_ACCESS_KEY_ID=your_aws_key_if_used
# AWS_SECRET_ACCESS_KEY=your_aws_secret_if_used
NODE_ENV=development
